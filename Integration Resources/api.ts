/**
 * API Configuration
 * 
 * Backend API for user sync, medication storage, and adherence tracking.
 * OCR and parsing happens on-device via ML Kit - no server needed for that.
 */

// FastAPI backend for users, meds, events, etc.
export const BACKEND_API_URL = "http://10.0.0.26:8000";

